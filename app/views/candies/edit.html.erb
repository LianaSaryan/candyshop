<h1>Move Candy</h1>
<p>To move a candy to a shelf, please enter the number of the shelf. Ex: '1', '2', '3', etc. and select 'Save Candy'.</p>
<p>To unshelf a candy, leave the following field blank and select 'Save Candy'. </p>
<%= form_for :candy, url: candy_path(@candy), method: :patch do |f| %>
	<% if @candy.errors.any? %>
		<% @candy.errors.full_messages.each do |msg| %>
			<div class = "aler alert-danger"><%= msg %></div>
		<% end %>
	<% end %>

	<p>
		<%= f.label :move_to_shelf %><br>
		<%= f.text_area(:belongs_to_shelf, {:class => 'form-control'}) %>
	</p>
  

	<p>
		<%= f.submit({:class => 'btn btn-primary'}) %>
	</p>
 
<% end %>
